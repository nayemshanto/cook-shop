const config = {
  plugins: ["@tailwindcss/postcss"],

  fontFamily: {
    Fredoka : "var(--font-fredoka-sans)",
    Poppins : "var(--font-poppins-sans)",
    Inter : "var(--font-inter-sans)",

  }
};

export default config;
